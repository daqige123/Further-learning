1.项目难点，尤其是文档解析怎么做的，怎样让 ai 返回的更好，不是通过 propmpt 限制输出特定结构，而是让 ai 更好的理解。
2.i18n 项目，为什么做，指标是啥，为啥选这个指标，为啥这么做，删除翻译效果能这么好（产物是 js 里面）
3.事件循环,非常简单
4.说出结果
let a = {
    name: 'byte dance',
    sayName() {
        console.log(this.name);
    }
}

const fb = a.sayName;
fb();
5.数组去重，不用 set，不用 map，只用一次循环呢，（用对象）
6.反问

